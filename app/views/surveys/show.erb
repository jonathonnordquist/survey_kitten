<%= Survey.where(id: @survey_id).first.title %>
<br>
<br>
<% @question.each_with_index do |question, question_no| %>
<form method="post" action="/responses">
  <%= question.text %>
  <br>
    <% @choices[question_no].each do |choice| %>
      <input type="radio" name="question[<%= question_no %>]" value="<%= choice.id %>"> <%= choice.option %>
      <br>
    <% end %>


  <% end %>
  <input type="submit">
</form>






